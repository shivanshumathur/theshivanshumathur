/**
 * Named assumptions for AI ROI Compass.
 * Phase 0: placeholders only. Real projection constants land in Phase 1+.
 */

/** How far ahead we calculate before giving up on payback. Wired in Phase 1. */
export const PROJECTION_WINDOW_MONTHS = 24;

/** Placeholder product name used by the Phase 0 shell. */
export const PRODUCT_NAME = "AI ROI Compass";

/** Placeholder tagline for the empty scaffold page. */
export const PRODUCT_TAGLINE =
  "A payback-timeline calculator for AI tool spend — built on the J-curve, not a spreadsheet fantasy.";
